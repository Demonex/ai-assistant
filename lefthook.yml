pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,ts,cjs,mjs,d.cts,d.mts,jsx,tsx,json,jsonc}"
      run: pnpm run lint
    format:
      glob: "*.{js,ts,jsx,tsx}"
      stage_fixed: true
      run: pnpm run format
